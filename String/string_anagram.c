// Check if 2 string are anagram(Same Character, Different Order)
// Condition:   ignore-->  Cases/Spaces
// Concept: Frequency Counting, ASCII handling; string preprocessing

#include <stdio.h>
#include <string.h>

int main()
{ char a[]= "Dirty room", b[]= "Dormitory";
    // 1.Convert all char to small case
      for (int i=0;)

    return 0;
}